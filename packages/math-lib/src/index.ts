import * as BigMath from "./bigmath";
import * as Engine from "./engine";
import * as Eval from "./eval";
import * as Parser from "./parser";

// Named exports
export { BigMath, Engine, Eval, Parser }

// Object as default export
export default { BigMath, Engine, Eval, Parser }
